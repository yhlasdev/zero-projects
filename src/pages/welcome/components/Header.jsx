import { Box } from "@mui/material";

const Header = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        justifyContent: 'space-between'
      }}>
    </Box>
  );
};

export default Header;